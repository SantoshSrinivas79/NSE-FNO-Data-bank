{
  "name": "nse-fno-data-bank",
  "version": "1.0.0",
  "private": true,
  "description": "Validated daily and historical NSE equity-derivatives bhavcopy downloader",
  "scripts": {
    "download": "node index.js",
    "db:build": "python3 scripts/build_sqlite.py",
    "db:status": "python3 scripts/build_sqlite.py --status",
    "test": "node --test && python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
